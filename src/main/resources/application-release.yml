spring:
  datasource:
    url: jdbc:mysql://localhost:3306/OperationsManagementSuiteDB
    username: rengu
    password: rengu
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  http:
    multipart:
      max-file-size: 1024MB
      max-request-size: 1024MB